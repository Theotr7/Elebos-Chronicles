{% extends 'base.html.twig' %}

{% block title %}Ajouter une carte
{% endblock %}

{% block body %}
	<h1>Nouvelle carte</h1>

	<div class="scroll-wrapper-card">
		<div class="scroll-parchment" style="background-image: url('{{ asset('images/ui/parchemin_test.png') }}');">
			<div class="scroll-content">
				{{ form_start(form) }}
				{{ form_widget(form) }}
				<button type="submit" class="btn-primary">Enregistrer</button>
				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}
